import 'package:flutter/material.dart';

class GamemodesScreen extends StatelessWidget {
  const GamemodesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Gamemodes Screen'),
    );
  }
}
